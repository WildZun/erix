const Discord = require("discord.js");
let db = require("quick.db")

exports.run = async (bot, message, args, guild) => {
    //EMBEDS
/*   const join = new Discord.MessageEmbed()
        .setAuthor("Nouvau serveur")
        .addField("ID du serveur", guild.id)
        .addField("Nom du serveur", guild.name)
        .addField("Nom du créateur du serveur", guild.owner)
        .setFooter("Erix- AddServer")
        .setTimestamp()
        .setColor("#E77F00")*/


    //s.channels.cache.get('834457753388318760').send("<@" + message.author.id + "> a exécuté la commande daily !")
    }

